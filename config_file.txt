{
  "proyecto": true //Si queremos ajustar la plantilla al proyecto, poner true. Si no, false.
  "dataset_language": "english", //english, spanish, german... (Indica el idioma de las columnas categóricas del dataset. Usado para el preproceso de texto)
  "algorithm": "KNN", //KNN, DecisionTree, RandomForest, NaiveBayes
  "metric_to_evaluate": "macro", //macro, micro o binary (binary quiere decir que usará métricas para clasificación binaria, por lo que también indica implicitamente si la clasificación es binaria o no)
  "hyperparametersKNN": {
    "p_min": 1,
    "p_max": 2,
    "k_min": 1,
    "k_max": 7,
    "w": ["uniform", "distance"], //Se evalúa tanto con uniform como distance. El usuario no debería tocar esto porque el programa decide cuál es el mejor modelo.
    "step": 2
  },
  "hyperparametersDecisionTree": {
    "min_depth": 1, //Número minimo de niveles que queremos que tenga nuestro arbol
    "max_depth": 10, //Número máximo de niveles que queremos que tenga nuestro arbol
    "min_samples_split": 10, //Número minimo de muestras que queremos que tenga una hoja para que se pueda dividir
    "min_samples_leaf": 5, //Controla que al hacer la division no se queden 1 o 2 muestras solas en una hoja
    "criterion": ["gini", "entropy"] //Se evalúa tanto con gini y entropy. El usuario no debería tocar esto porque el programa decide cuál es el mejor modelo.
  },
  "hyperparametersRandomForest": {
    "n_estimators": 10 //Cantidad de árboles a generar
  },
  "hyperparametersNaiveBayes": {
  "tipo": "categorical" //categorical, gaussian, multinomial. Indica el tipo de motor de Naive Bayes a utilizar. Usaremos "categorical" si discretizamos columnas continuas o no existen columnas continuas y todas son categoricas.
                        //Usaremos "gaussian" si hay muchas columnas continuas.
                        //Usaremos "multinomial" si queremos contar cantidades. Un muy buen ejemplo es el de búsqueda de Spam-Ham en mensajes.
                        //Usaremos "complement" si procesamos texto (frecuencias o TF-IDF) pero tenemos clases muy DESBALANCEADAS (ej. 700 positivos y 50 neutros), sin necesidad de inventar datos con SMOTE.
  "min_alpha": 0.01, //Suavizado de Laplace
  "max_alpha": 1, //Suavizado de Laplace
  "step_alpha": 0.01 //longitud de los saltos de min a max

  },
  "preprocessing": {
    "agrupar_sentimiento_proyecto": true //Si es True quiere decir que las columnas de sentimientos son numéricas y no textuales y queremos convertirlas a textuales

    "separator": "," //Separador de los datos en el CSV. Actualmente "," porque es lo que más se usa, pero en el de SPAM/HAM se usas "\t" porque está separado por un tabulador.
    "drop_features": [], //Se meten los nombres de las columnas que se quiere eliminar y no se van a usar
    "continuous_features_discretize": [], //Se meten los nombres de las columnas que son CONTINUAS y se quieren DISCRETIZAR. Usado para el NAIVE BAYES. Dejar vacío si no se quiere discretizar nada
    "discretize_bins": 5, //Número de rangos/intervalos a hacer. Usado en conjunto con la característica anterior.
    "categorical_features_convert": [], //Se meten los nombres de las columnas categóricas que SÍ hay que meter como TF-IDF/BOW
    "missing_values": "impute", //impute, delete
    "impute_strategy": "mean", //median, mean, most_frequent
    "scaling": "max-min", //max-min, max, standard/z-score (Cualquier otra cosa si no se quiere escalar) En Naive Bayes y DecisionTree/RandomForest NO hay que escalar.
    "features_scale": [], //Se meten los nombres de las columnas que se quieran escalar. Si se deja vacío pero hay un escalador puesto, se escalan todas las columnas. Si no hay escalador, ninguna se escala.

    "text_preprocess": "tf-idf", //tf-idf, one-hot, frequency (Si usamos tf-idf NUNCA escalar valores, porque ya están escalados y gasta muchísima RAM). Poner cualquier otra cosa si no se quiere adaptar
    "ngramas_tfidf": true, //Si queremos usar monogramas (1), bigramas (2), trigamas (3)... en TF-IDF
    "balance": 0.4, //Indica el ratio de balanceo del dataset. 0.4 indica que el 40% de los datos será de la clase minoritaria. Si es multiclass usar "auto"
    "sampling": "undersampling" //undersampling, oversampling (Cualquier otra cosa si no se quiere hacer balanceo)
  }
}